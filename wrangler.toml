name = "xovato"                          # Your project name
compatibility_date = "2025-12-01"        # Or newer, e.g. "2026-01-01" – important for Node.js APIs
compatibility_flags = ["nodejs_compat", "nodejs_compat_v2"]  # Top-level, not under [site]

main = ".open-next/worker.js"            # Adapter's entry point (check your .open-next after local build)

[assets]
directory = ".open-next/assets"
binding = "ASSETS"

# Optional: If you add caching later (KV/R2), put here too
